# Templating

OCaml library for executing code blocks in markdown-style templates and replacing them with their output.

Processes triple-backtick delimited code blocks (` ``` `), executes them, and substitutes the result inline.

